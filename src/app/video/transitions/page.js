import TransitionLibrary from "@/features/vibecut/library/TransitionLibrary";

export const metadata = {
  title: "Transitions",
  robots: { index: false, follow: false },
};

export default function TransitionLibraryPage() {
  return <TransitionLibrary />;
}
