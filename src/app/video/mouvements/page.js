import MotionLibrary from "@/features/vibecut/library/MotionLibrary";

export const metadata = {
  title: "Mouvements",
  robots: { index: false, follow: false },
};

export default function MotionLibraryPage() {
  return <MotionLibrary />;
}
