import SpacePlaceholder from "@/features/vibeos/shell/SpacePlaceholder";

export const metadata = {
  title: "Layout",
};

export default function CreerLayoutVisuelPage() {
  return (
    <SpacePlaceholder title="Layout" phase="B">
      Compose tes visuels : formats, modèles, templates et habillages. Cet espace
      est en construction — l&apos;ancien éditeur reste disponible sur /studio.
    </SpacePlaceholder>
  );
}
