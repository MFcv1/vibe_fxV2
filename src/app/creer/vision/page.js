import SpacePlaceholder from "@/features/vibeos/shell/SpacePlaceholder";

export const metadata = {
  title: "Vision",
};

export default function CreerVisionPage() {
  return (
    <SpacePlaceholder title="Vision" phase="C">
      Sublime tes photos de téléphone : amélioration automatique et looks sûrs.
      Cet espace est en construction — l&apos;ancien Vision reste disponible sur /studio.
    </SpacePlaceholder>
  );
}
